export default class ListItem {
    id: number
    list_id: number
    name: string
    count: number

    constructor(id: number, list_id: number, name: string, count: number) {
        this.id = id
        this.list_id = list_id
        this.name = name
        this.count = count
        this.count = count
    }
}