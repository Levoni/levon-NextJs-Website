import ResetStart from "@/components/password_reset_start"

export default async function ResetStartPage() {

  return (
      <div style={{flex:'1', display:'flex', justifyContent:'center'}}>
        <ResetStart></ResetStart>
      </div>
  )
}
